# Transformer Builder Colab Template - Requirements
# Minimal dependencies - leverages Colab's pre-installed packages
# Version: 3.1.0
# Last Updated: 2025-01-13

# ==============================================================================
# CRITICAL: Use --no-binary and specific install order to prevent numpy corruption
# Strategy: Install packages one by one to avoid pip trying to "fix" dependencies
# ==============================================================================

# HuggingFace ecosystem (install FIRST - no numpy deps)
datasets>=2.16.0,<3.0.0
tokenizers>=0.15.0,<1.0.0
huggingface-hub>=0.20.0,<1.0.0

# Utilities (safe - no numpy conflicts)
torchinfo>=1.8.0,<3.0.0

# Export formats (safe - no numpy conflicts)
onnx>=1.15.0,<2.0.0
onnxruntime>=1.16.0,<2.0.0

# Training optimization (safe)
optuna>=3.0.0,<4.0.0

# Development (optional)
pytest>=7.4.0,<8.0.0
pytest-cov>=4.1.0,<5.0.0

# ==============================================================================
# INSTALL SEPARATELY WITH --no-deps (to prevent numpy corruption):
# - pytorch-lightning (see installation cell for manual install)
# - torchmetrics (dependency of lightning)
# - accelerate (has numpy constraints)
# ==============================================================================

# ==============================================================================
# COLAB PRE-INSTALLED (DO NOT REINSTALL):
# - numpy 2.x (corrupts if reinstalled!)
# - torch 2.6-2.8
# - transformers 4.37+
# - pandas, matplotlib, seaborn, plotly
# - scikit-learn, scipy
# - tensorflow, jax
# - opencv, PIL
# - tqdm, ipywidgets, fsspec, pyyaml, packaging, typing-extensions
# ==============================================================================
